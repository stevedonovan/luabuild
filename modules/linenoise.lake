-- in this case, we'll assume that the Lua core has always been statically linked
-- against linenoise

return luabuild.library{'linenoise',src='linenoise',
    incdir = PWD..'linenoise' -- PWD is luabuild directory
}
